# Optimization

The initial optimizer searches a configurable marketing-spend range and keeps only feasible solutions that satisfy a minimum runway constraint.

Objective:

- maximize expected profit under the baseline scenario assumptions

The current implementation is intentionally simple and transparent. It can later be replaced by a more general optimization solver as additional domains and constraints are introduced.
