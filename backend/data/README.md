# Demo data

`demo_monthly_data.csv` is synthetic demonstration data for a B2B SaaS growth scenario. It is included to make the project reproducible without external data dependencies.

The data should not be presented as a real company's financial record.
